spring:
  application:
    name: company-service

  config:
    import: "config-server:http://localhost:8888"

  datasource:
    url: jdbc:postgresql://postgres:5432/company_db
    username: aliya
    password: aliya123

server:
  port: 8082